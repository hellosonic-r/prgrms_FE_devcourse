import React from 'react';
import styled from 'styled-components';

const LoginPageWrapper = styled.div`
  height: 100vh;
`;
const LoginPage = () => {
  return <LoginPageWrapper>LoginPage</LoginPageWrapper>;
};

export default LoginPage;
