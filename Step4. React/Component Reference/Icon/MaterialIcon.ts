// const MaterialIcon = ({ name, size = 16, color = '#000', ...props }) => {
//   const className = `material-icons ${props.className || ''}`;

//   return (
//     <i className={className} style={{ fontSize: size, color }} {...props}>
//       {name}
//     </i>
//   );
// };

// export default MaterialIcon;
